using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MeleeGroupController : GroupController
{
    // Start is called before the first frame update
    void Start()
    {
        Cost = 250;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
